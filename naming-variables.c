#include <stdio.h>

int main(void)
{
int number; // i fixed the name of the first variable by deleting (!)
int test_value; // I fixed the name of the second variable by spacing (_)
int Example1; // I fixed the name of the third variable by deleting 1 and Replaced it with an uppercase letter
float int;
printf("Variables created successfully\n"); // I fixed the name of the second variable by deleting the dot(.)
}

